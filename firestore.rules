rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir la lectura pública de todos los documentos.
    // La escritura se deniega por defecto para proteger los datos.
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
