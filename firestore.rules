
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permite la lectura pública para que todos los usuarios puedan ver el catálogo y los consejos.
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
